{
  "name": "middlesville-trusted-loans-backend",
  "version": "1.0.0",
  "description": "Backend API and admin auth for Middlesville Trusted Loans.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node scripts/migrate.js",
    "test": "node --check server.js && node --check db.js && node --check mail.js && node --check router/auth.js && node --check router/admin.js && node --check router/applications.js && node --check middleware/auth.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0",
    "mysql2": "^3.23.4",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
